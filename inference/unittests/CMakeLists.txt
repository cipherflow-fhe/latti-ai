include(CTest)
enable_testing()

add_executable(test_data_structs
  test_data_structs.cpp
  ut_util.cpp
)
target_link_libraries(test_data_structs inference_task)
add_test(NAME test_data_structs COMMAND test_data_structs)

# Copy test_gen_layers.py to build directory
configure_file(
    ${CMAKE_CURRENT_SOURCE_DIR}/test_gen_layers.py
    ${CMAKE_CURRENT_BINARY_DIR}/test_gen_layers.py
    COPYONLY
)

add_executable(test_fhe_layers_hetero test_fhe_layers_hetero.cpp ut_util.cpp)
target_link_libraries(test_fhe_layers_hetero PUBLIC fhe_layers lattisense)

if(INFERENCE_SDK_ENABLE_GPU)
    target_compile_definitions(test_fhe_layers_hetero PRIVATE INFERENCE_SDK_ENABLE_GPU)
endif()

add_test(NAME test_fhe_layers_hetero COMMAND test_fhe_layers_hetero)

# End To End test
# Test data is generated by running test_compiler.py::TestE2E
add_executable(test_e2e test_e2e.cpp)
target_link_libraries(test_e2e PUBLIC
    -Wl,--start-group
    inference_interface
    inference_task
    fhe_layers
    log
    -Wl,--end-group
    lattisense
    ${HDF5_CXX_LIBRARIES}
)

if(INFERENCE_SDK_ENABLE_GPU)
    target_compile_definitions(test_e2e PRIVATE INFERENCE_SDK_ENABLE_GPU)
endif()

add_test(NAME test_e2e COMMAND test_e2e)

# ── Softmax layer unit test ───────────────────────────────────────────────────
# 与 test_fhe_layers_hetero 保持一致：链接 fhe_layers（含 SoftmaxLayer 实现）
# 和 lattisense（CKKS 上下文、密钥操作等）
add_executable(test_softmax_layer test_softmax_layer.cpp ut_util.cpp )
target_include_directories(test_softmax_layer PRIVATE
    ${CMAKE_SOURCE_DIR}/inference/fhe_layers
)
target_link_libraries(test_softmax_layer PUBLIC fhe_layers lattisense)
add_test(NAME test_softmax_layer COMMAND test_softmax_layer)

# ── Softmax layer base (无自举) 单元测试 ─────────────────────────────────────
add_executable(test_softmax_layer_base test_softmax_layer_base.cpp ut_util.cpp)
target_include_directories(test_softmax_layer_base PRIVATE
    ${CMAKE_SOURCE_DIR}/inference/fhe_layers
)
target_link_libraries(test_softmax_layer_base PUBLIC fhe_layers lattisense)
add_test(NAME test_softmax_layer_base COMMAND test_softmax_layer_base)

add_executable(test_n65536_smoke test_n65536_smoke.cpp)
target_link_libraries(test_n65536_smoke PUBLIC lattisense)
